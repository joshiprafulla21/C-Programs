/*Write a program to count the no of characters in the input*/

#include<stdio.h>

int main()
{	
	long nc;
	int c,k;
	
	while((c=getchar())!=EOF)
	{
		++nc;
		k=nc;
		
	}
	printf("NC:%ld\n",nc);
	return 0;
}
